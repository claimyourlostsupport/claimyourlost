/** Listing photo size (px). Keep in sync with server `IMAGE_MAX_EDGE` (max 200). */
export const LISTING_IMAGE_SIZE = 200;

export const listingImgClass =
  'w-[200px] h-[200px] max-w-full max-h-full object-cover shrink-0 rounded-lg';
